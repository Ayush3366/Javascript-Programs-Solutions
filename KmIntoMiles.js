// taking input from the user
const km = prompt("Enter value in kilometers: ");

// conversion factor
const convert = 0.621371;

// calculate miles
const miles = km * convert;

console.log(`${km} km is equal to ${miles} miles.`);
